import React from 'react';
import {StyleSheet} from 'react-native';
import {BaseColor} from '@config';

export default StyleSheet.create({
  swiperContain: {
    width: '100%',
    height: 200,
  },
  contain: {
    flex: 1,
  },
  contentPage: {
    bottom: 0,
  },
  img: {
    width: '100%',
    height: 200,
  },
  priceContain: {position: 'absolute', right: 0},
  infoContain: {
    padding: 20,
    // backgroundColor: 'red',
    // borderRadius: 4,
    // shadowColor: '#000000',
    // shadowOffset: {width: 0, height: 2},
    // shadowOpacity: 0.9,
    // shadowRadius: 6,
    // elevation: 5,
    // width: '100%',
  },
});
